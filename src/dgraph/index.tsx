import React from 'react';
import GraphVizLayout from './react-components/graphviz';

const FlowGraph = () => {
    return <GraphVizLayout width={1000} height={800} type="fn-node-view" />;
};

export default FlowGraph;
